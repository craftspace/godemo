# vLink

# Install

    npm install -g grunt-cli
    npm install -g bower
    npm install && bower install

# Run
    
    grunt serve        // development
    grunt serve:dist   // production

# Build

    npm build

# Test

    npm test

# Build war

    grunt release

# Documentation
